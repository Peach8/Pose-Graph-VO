clc;

% adding data folder to the path
% cd('../../data')
% addpath(genpath(pwd))
% adding the dev folder to thr path
% cd('../dev/matlab')
addpath(genpath(pwd))
% shuffling the random number generator seed
rng('shuffle')